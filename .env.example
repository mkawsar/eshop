# Database configuration (used by app/etc/env.php)
# Copy this file to .env and set your values.
# When .env is missing, defaults are: 127.0.0.1, eshop, root, password

DB_HOST=mysql
DB_NAME=eshop
DB_USERNAME=root
DB_PASSWORD=password
